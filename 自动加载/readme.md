##### 自动加载原理

```
// 通过命名空间+自动加载函数 实现文件的自动引入

// 首先 通过spl_autoload_register函数进行注册自动加载函数，当new一个未包含的类时，不会先报致命错误，而是通过自动加载函数尝试引入该类，结合命名空间的使用，可以获取该类的完整命名空间，然后通过解析命名空间引入该类文件，要注意命名空间得和路径名相匹配，如不匹配，则要预先添加好映射关系

```

